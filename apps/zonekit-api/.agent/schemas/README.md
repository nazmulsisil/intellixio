# ZoneKit Schemas (Context Files)

This folder contains authoritative contracts for ZoneKit:
- GeoJSON input contract (TypeScript)
- DB schema contract snapshot (Postgres/PostGIS)

Update these files whenever ZoneKit DB shape or ingestion contract changes.
